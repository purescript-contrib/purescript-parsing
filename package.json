{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && pulp test"
  },
  "devDependencies": {
    "pulp": "^12.4.2",
    "purescript-psa": "^0.7.3",
    "purescript": "^0.12.5",
    "rimraf": "^2.6.3"
  }
}
