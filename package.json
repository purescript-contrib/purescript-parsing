{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && pulp test"
  },
  "devDependencies": {
    "pulp": "^13.0.0",
    "purescript-psa": "^0.7.3",
    "purescript": "^0.13.3",
    "rimraf": "^3.0.0"
  }
}
