{
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "build": "pulp build && pulp test"
  },
  "devDependencies": {
    "pulp": "^14.0.0",
    "purescript-psa": "^0.7.3",
    "rimraf": "^3.0.2"
  }
}
